import styled from "styled-components";

export const SmallLink = styled.a`
  color: ${({ theme }) => theme.colors.primary};
  font-size: 0.875rem;
  margin-top: 1rem;
  text-align: center;
  display: block;
`;
